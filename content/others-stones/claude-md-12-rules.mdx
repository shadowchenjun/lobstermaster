---
title: "CLAUDE.md 规则从 4 条增加到 12 条，Claude 错误率从 41% 降到 3%"
date: "2026-05-16"
category: "others-stones"
coverEmoji: "📋"
tags: ["CLAUDE.md", "AI编程", "工程实践", "Agent", "Claude Code"]
summary: "Andrej Karpathy 的 4 条规则是起点，Mnilax 历经 6 周、30 个代码库、50 个标准任务，将规则扩展到 12 条，错误率从 41% 压到 3%。每条规则背后都有一个真实翻车现场。"
source: "https://mp.weixin.qq.com/s/-otRArJp28nPaPhHQQ1_4Q"
---

## 背景

2026 年 1 月，Andrej Karpathy 发了一条长帖吐槽 Claude 写代码的三个问题：静默错误假设、过度复杂化、波及非目标代码。Forrest Chang 把这些问题提炼成了 4 条行为规则，放进 CLAUDE.md 文件，传到了 GitHub。文件发布第一天 5582 颗星，两周 6 万书签，到今天 12 万星——2026 年增长最快的单文件仓库。

5 月，Mnilax 发现 Karpathy 的模板是为 1 月的代码编写问题设计的。5 月的 Claude Code 生态已经有了新的病灶——Agent 冲突、Hook 级联、Skill 加载冲突、跨会话断裂的多步工作流。于是他做了件很硬核的事：**30 个代码库，50 个标准任务，6 周持续测试**，在原版 4 条的基础上，一条一条补齐了 8 条新规则。

---

## 一、CLAUDE.md 是 AI 编程栈里最被低估的文件

大多数开发者的 CLAUDE.md 处于三种状态：

- 什么都往里塞，膨胀到 4000+ token，遵循率掉到 30%
- 完全跳过，每次手动 Prompt——5 倍 Token 浪费，会话之间零一致性
- 复制一个模板就忘了，用两周还行，代码库一变就静默失效

Anthropic 的官方文档说得很清楚：CLAUDE.md 是建议性的，Claude 大约遵循 80%。**超过 200 行，遵循率急剧下降**——因为重要规则被埋在了噪音里。

---

## 二、原版 4 条规则——底线，不是天花板

**规则 1——先想后写。** 杜绝隐蔽假设。明确声明你的前提假设，阐明方案权衡。有疑问先确认，切忌盲目猜测。若存在更简单的实现路径，应果断提出异议。

**规则 2——简单至上。** 仅以最少代码解决问题。不添加"以防万一"的冗余功能，不为仅用一次的代码强行设计抽象层。若资深工程师会认为其过度复杂，则立即简化。

**规则 3——外科手术式修改。** 仅改动绝对必要的部分。切勿"顺手优化"相邻代码、注释或格式排版。未出问题的代码绝不重构。严格贴合项目现有代码风格。

**规则 4——目标驱动。** 明确定义成功标准（验收条件）。持续迭代直至验证通过。不要规定具体操作步骤，只需清晰描述"成功的最终形态"，交由模型自主探索与迭代。

> 这四条规则大约能覆盖 ~40% 的失败模式，而剩余约 60% 的问题存在于下述空白地带中。

---

## 三、补齐的 8 条规则

### 规则 5：模型只做判断，不做决策

仅将模型用于需要判断与裁量的场景：

- ✅ 适用：分类、内容起草、摘要总结、非结构化文本信息提取
- ❌ 禁用：路由分发、重试机制、状态码处理、确定性数据转换

> **翻车现场：** 一段调用 Claude 去"判断 503 该不该重试"的代码，稳定运行两周，然后开始出问题——因为模型开始把请求体内容当成决策的上下文。重试策略变成了随机的。

### 规则 6：硬性 Token 预算，没有例外

- 单任务预算：4,000 Token
- 单会话预算：30,000 Token
- 任务接近预算上限时，立即执行摘要总结并重置上下文

> **翻车现场：** 某次调试会话持续了整整 90 分钟。模型毫无自觉地对着同一条 8KB 的错误日志反复迭代，逐渐丢失了上下文记忆。到对话末尾，它居然开始建议我早在 40 条消息前就已明确否决的方案。若有 Token 预算限制，它在第 12 分钟就会被强制熔断。

### 规则 7：暴露冲突，不要折中

若代码库中既有的两种模式相互矛盾，切勿强行融合。明确选择其一，阐明选择理由，并将另一种标记为待清理项。

> **翻车现场：** 某代码库中并存着两种错误处理模式：async/await + try/catch 的局部处理，以及全局错误边界的集中拦截。Claude 写出的新代码将两者强行叠加。错误处理逻辑直接翻倍，异常被重复捕获并静默"吞掉"两次。

### 规则 8：先读后写

在文件中追加代码前，务必通读该文件的导出接口、直接调用方，以及任何显而易见的公共工具函数。

> **翻车现场：** Claude 在一个已有相同功能的函数旁边加了一个新函数——它没读到那个已有函数。两个函数做一样的事，新函数因为导入顺序而优先执行。

### 规则 9：测试是必选项，但绝非目标本身

每个测试都必须明确体现该行为为何重要（WHY），而不仅仅是断言它做了什么（WHAT）。

> **翻车现场：** Claude 为某个认证函数编写了 12 个测试，且全部通过。然而生产环境的认证功能却彻底失效。这些测试仅验证了函数"是否返回了数据"，而非"是否返回了正确数据"。

### 规则 10：长任务必须设检查点

在执行多步骤任务时，每完成一个关键节点后必须执行状态同步：明确总结已实施操作、已验证结果，以及剩余待办事项。

> **翻车现场：** 一个 6 步重构在第 4 步出了问题。等发现时，Claude 已经在错误状态上继续完成了第 5 和第 6 步。拆解比整个重做还花时间。检查点机制会在第 4 步就抓住问题。

### 规则 11：约定胜于新奇

若项目使用蛇形命名（snake_case）而你偏好驼峰（camelCase）：请使用蛇形。技术分歧可另开议题讨论。但在代码库内部，**规范一致性 > 个人技术偏好**。

> **翻车现场：** Claude 在一个 class 组件的代码库里引入了 React hooks。它们能跑，却也搞坏了代码库的测试模式——测试假定有 componentDidMount。花了半天时间来删除和重写。

### 规则 12：显性失败，不要静默

若无法确信操作已正确执行，必须明确声明。若有 30 条记录被静默跳过，宣称"迁移完成"便是错误结论。

> **翻车现场：** Claude 说一次数据库迁移"成功完成"。它悄悄跳过了 14% 因为约束冲突没处理的记录。11 天后报告数据开始对不上时才发现。

---

## 四、实测数据：从 41% 到 3%

| 配置 | 规则数 | 错误率 | 遵循率 |
|------|--------|--------|--------|
| 无规则 | 0 | 41% | — |
| 4 条规则 | 4 | — | 78% |
| **12 条规则** | **12** | **3%** | **76%** |

真正值得关注的结果：规则从 4 条扩展至 12 条，几乎未增加合规执行开销（78% → 76%），却将错误率进一步降低了 38 个百分点。新增的规则覆盖了原始 4 条规则未能触及的失败模式，它们形成了**互补增强**。

---

## 五、原版 4 条规则的局限性

即便尚未引入增量规则，Karpathy 的模板在以下四个维度也已显露出它的局限性：

1. **长任务 Agent** — 缺乏预算约束、状态检查点以及"显式报错"机制，任务流在长时间运行中极易发生语义漂移
2. **多代码库一致性** — 原生规则缺乏决策优先级逻辑，在包含数十个微服务的 Monorepo 中，模型往往产出一种平庸的风格"平均值"
3. **测试质量** — 系统简单地将"测试通过"与"任务成功"画等号，却忽略了测试本身的有效性与完备性
4. **生产 vs 原型** — 旨在防止生产环境过度设计的约束，在原型阶段反而可能成为拖累

---

## 核心启示

CLAUDE.md 不是越多越好。超过 200 行，遵循率急剧下降。12 条规则之所以有效，在于它们：

- 每条都来自真实翻车场景
- 互补增强，不争夺同一份注意力预算
- 简洁、明确、可操作

> **底线不是天花板。** 4 条规则是起点，12 条规则是经过实测验证的当前最优解。但你的代码库可能还需要更多。

---

*原文来源：[微信公众号](https://mp.weixin.qq.com/s/-otRArJp28nPaPhHQQ1_4Q)*
